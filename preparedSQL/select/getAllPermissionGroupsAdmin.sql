SELECT PERMISSION_GROUPS.PERMISSION_NAME,PERMISSION_GROUPS.PERMISSIONGROUP_DESCRIPTION, (SELECT USERNAME FROM USERS WHERE USER_ID = PERMISSION_GROUPS.PERMISSION_GROUP_ADMIN) AS GROUPADMIN_USERNAME FROM PERMISSION_GROUPS
WHERE 1